[pytest]
norecursedirs = venv* .*
addopts =
    -v
    -p no:warnings
    -r fEsxXw
    -vvv
    --doctest-modules
    --ignore setup.py
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
    --cov=theolex_document_processing
